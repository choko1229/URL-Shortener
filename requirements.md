# 短縮URLサービス（chok.ooo）要件定義書

- 作成日: 2026-09-17
- ステータス: 要件定義完了（一部インフラ確認事項あり）
- 次フェーズ: 設計（テーブル設計・ディレクトリ構成・API仕様策定）
- 更新日: 2026-09-22（実装は v26.9.5 で完了。1〜11章は当初の要件のまま残し、実装との差異を「12. 実装との差異」に追記）

---

## 1. サービス概要

| 項目 | 内容 |
|---|---|
| 想定利用者 | 個人利用＋知人への提供。不特定多数向けの大規模公開は想定しない |
| 目的 | 自分の他プロジェクト（BOOTHまとめサイト等）からのAPI経由発行、および個人での利用 |
| ドメイン | `chok.ooo`（取得済み） |
| サービス名 | chok.ooo |

### 1-1. ドメイン構成

| サブドメイン | 役割 |
|---|---|
| `chok.ooo` | 短縮URL発行フォーム／短縮URLへのアクセス受付（`chok.ooo/abc123`） |
| `dash.chok.ooo` | ユーザーダッシュボード（発行履歴・統計閲覧・管理） |
| `api.chok.ooo` | 外部（自プロジェクト限定）向けAPI |
| `redirect.chok.ooo` | リダイレクト確認・悪意URLチェック専用 |

**アーキテクチャ方針**: 上記4サブドメインは**単一のLaravelアプリケーション**として実装し、PHP側（サブドメインルーティング）で制御する。別々のデプロイ・別アプリには分離しない。

---

## 2. 短縮URL機能

### 2-1. 短縮コード仕様

| 項目 | 仕様 |
|---|---|
| ランダムコード桁数 | **7桁**（英数字、大文字小文字区別なしで重複判定） ※衝突確率計算により決定。10万件発行時でも衝突確率0.14%程度 |
| カスタムスラッグ | ログインユーザーのみ利用可。3〜20桁、**大文字小文字を区別する** |
| 発行方式 | 未ログイン: ランダム自動生成のみ／ログイン後: カスタムスラッグも選択可 |
| 同一URLの再発行 | 常に新規コードを発行（重複排除なし。毎回別の短縮URLになる） |

### 2-2. 予約語（カスタムスラッグとして使用禁止）

DB管理とし、ダッシュボードから追加可能。初期リストは以下。

```
システム関連: api, admin, kanri, dashboard, dash, redirect, www, mail, ftp, cdn,
              app, static, assets, cron, cli, root, system, sys
認証関連: login, logout, register, signup, signin, signout, auth, oauth, callback,
          password, reset, verify, token
機能関連: help, support, about, contact, terms, privacy, policy, faq, docs, blog,
          news, status, health, ping, test, debug
紛らわしい語: null, undefined, none, delete, remove, error, 404, 500,
              favicon, robots, sitemap, index, home
個別追加: choko1229, choko, 1229, choco, tyoko
```

※ **管理者(admin)は予約語であってもカスタムスラッグとして使用可能**とする。

### 2-3. 有効期限

| 対象 | 仕様 |
|---|---|
| ログインユーザー | 発行時に有効期限を自由に設定可能（日時指定）。「無期限」も選択可 |
| 未ログインユーザー | 有効期限は必須、**最大30日**まで。「無期限」を選択しようとするとログインを促すポップアップを表示 |
| 期限切れ時の挙動 | 404ではなく**専用の期限切れページ**を表示 |

### 2-4. 削除・編集

| 項目 | 仕様 |
|---|---|
| 編集 | カスタムスラッグのみ編集可能（元URL等は編集不可） |
| 削除 | 可能。削除された短縮コードは**永久に欠番**とし、再利用しない |
| 削除権限 | ログインユーザー: 自分が発行したものを削除可／未ログインユーザー: 発行時に払い出される**削除用シークレットトークン**を用いて削除／管理者: 全URLを削除可能 |
| データ保持 | 削除・期限切れ後も、元々設定されていたURL情報は記録として**DBから物理削除しない**（論理削除） |

### 2-5. パスワード保護

| 項目 | 仕様 |
|---|---|
| 利用可否 | 未ログインユーザーも含め**誰でも設定可能** |
| ブルートフォース対策 | パスワード誤入力**5回まで**で**15分間ロックアウト** |

### 2-6. 統計・アクセス解析

| 項目 | 仕様 |
|---|---|
| 記録内容 | クリック数、リファラ、国、デバイス種別 |
| 記録方法 | GA4は不採用（リダイレクトのみでJSページビューが発火しないため）。**サーバーサイドでDBに直接記録**（`$_SERVER`からUser-Agent・リファラ取得、IPからGeoIP判定） |
| 閲覧権限 | ログインユーザー: 自分が発行したURLの統計をダッシュボードで閲覧可／未ログインユーザー発行分: 本人は閲覧不可だが、**データ自体は記録し管理者のみ閲覧可能** |

### 2-7. 悪意URLチェック

| 項目 | 仕様 |
|---|---|
| チェックタイミング | **リダイレクトのたびに毎回**実施 |
| キャッシュ | 同一URLへのチェック結果は**3日間キャッシュ** |
| 想定API | Google Safe Browsing API（無料枠あり、フィッシング/マルウェアDB） |

---

## 3. リダイレクトフロー

```
1. ユーザーが chok.ooo/abc123 にアクセス（GET）
2. chok.ooo が該当コードをDB検索
3. 存在すれば、中間ページ（JavaScript使用）を返却
   - パスワード保護ありの場合: ここでパスワード入力を求める（案1採用）
4. 中間ページのJSが redirect.chok.ooo へ自動POST送信
   （短縮コード or 認証済み情報を送信）
5. redirect.chok.ooo が受信し、画面に以下を表示：
   - リダイレクト先URL（元URL）の表示
   - 悪意URLチェックのプログレス表示（Safe Browsing API等を非同期実行）
6. チェック完了後、自動的に元URLへリダイレクト
```

※ 中間ページでのJavaScript使用、kagoyaサーバーで利用可能な技術の活用は許可済み。

---

## 4. 認証・ユーザー管理

### 4-1. ログイン方式

| 項目 | 仕様 |
|---|---|
| 認証方式 | **Discordログインのみ**（他の認証方式は併用しない） |
| 取得スコープ | `identify`（ユーザー名・アイコン等、登録に必要な最小限のみ）。`email`・`guilds`は取得しない |
| サーバー参加制限 | 特定Discordサーバーのメンバーのみ、という制限は**設けない** |

### 4-2. 管理者権限

| 項目 | 仕様 |
|---|---|
| 初期セットアップ | WordPressのような**初期セットアップウィザード形式**。初回アクセス時にDiscord OAuth2のClient ID/Secret、DB接続情報を画面から入力 |
| 初代管理者 | セットアップ完了後、**最初にDiscordログインしたユーザー**が自動的にadminになる |
| 2人目以降の管理者 | 既存の管理者がダッシュボード上で対象ユーザーを「admin昇格」させる機能を提供 |
| 管理者の人数 | 複数人を想定 |
| 管理者の特権 | 予約語の使用可、全URL管理・削除、統計閲覧、APIキー発行、アップデート管理 |

### 4-3. 利用制限

| 対象 | 発行上限 | レート制限 |
|---|---|---|
| ログインユーザー | 60件/月 | 5回/分 |
| 未ログインユーザー（IPベース） | 5件/月 | 1回/3分 |

### 4-4. その他ユーザー機能

- マイページ（発行履歴・統計閲覧）: 必要
- 退会・アカウント削除機能: 必要
- スパム対策: reCAPTCHA導入

---

## 5. API仕様

| 項目 | 仕様 |
|---|---|
| 認証方式 | APIキー方式（ダッシュボードから発行） |
| 利用可能範囲 | **管理者のみ**利用可能（自分が管理する他プロジェクトからの発行専用） |
| 外部（第三者）への開放 | なし |
| レート制限 | **不要**（管理者専用のため） |

---

## 6. QRコード生成

短縮URL発行時にQRコードを生成する機能を提供する（想定ライブラリ: `endroid/qr-code`）。

---

## 7. 自動アップデート機構

### 7-1. 方式

**案A: 完全自動適用**を採用。ただし事故防止のため以下の安全装置を実装する。

```
【自動アップデート処理フロー】
1. cronが1日1回、GitHub Releasesをチェック
2. 新しいリリース（vYY.MM.patch 形式、例: v26.9.0）を検知
3. 実行前に自動バックアップを取得
   - DBダンプ（mysqldump）
   - 現在のコード一式
   - 過去3世代分を保持（4世代目以降は自動削除）
4. git fetch → 新タグへcheckout
5. composer install --no-dev
6. DBマイグレーション実行
7. ヘルスチェック実行
   - DB接続確認
   - 主要な短縮URL発行機能の動作確認（実際に仮の短縮URLを発行してみるテスト等）
8a. ヘルスチェックOK → 更新完了、Discord Webhookで管理者に「更新成功」通知
8b. ヘルスチェックNG → 自動ロールバック実行
    - コード: 直前のタグへgit checkout
    - DB: バックアップから復元
    - Discord Webhookで管理者に「更新失敗・ロールバック実施」を通知
```

### 7-2. リポジトリ・認証情報管理

| 項目 | 仕様 |
|---|---|
| リポジトリ | GitHub、**プライベートリポジトリ** |
| バージョン基準 | GitHub Releaseタグ（`vYY.MM.patch` 形式、例: v26.9.0） |
| デプロイキー（GitHubトークン）の保管 | `.env`には**Laravel標準の暗号鍵（APP_KEY）のみ**配置。GitHubトークン自体はAPP_KEYで暗号化した状態で**DBに保存** |

### 7-3. バックアップ方針

| 項目 | 仕様 |
|---|---|
| 保存先 | サーバー内の別ディレクトリ（外部ストレージへの退避は現時点では不要） |
| 保持世代数 | 直近3世代分 |
| チェック頻度 | 1日1回 |

---

## 8. 技術・インフラ

| 項目 | 内容 |
|---|---|
| 言語・FW | PHP 8.2 + **Laravel 12.x**（2026-09-17 変更: 11.x はサポート終了かつ CVE-2026-48019 が未修正のため） |
| DB | MySQL 8.0 |
| サーバー | kagoyaレンタルサーバー |
| SSL | ユーザー側で設定済み・設定予定（kagoya側） |
| フロントエンド | Laravel Blade + 必要に応じてJavaScript（中間ページの自動POST・プログレス表示等） |

### ⚠️ 未確認のインフラ依存事項（要ユーザー確認）

自動アップデート機構（第7章）の実現には以下が**必須**だが、現時点で未確認：

1. **Composerの利用可否**（kagoya管理画面またはSSH経由）
   - 利用不可の場合、ローカルでComposerインストール後にvendorフォルダごとアップロードする代替運用が必要
2. **cronの利用可否**（自動アップデートチェック・期限切れURL処理等に必須）
   - 「可能だったはず」という認識のため、kagoyaコントロールパネルでの正式確認が必要
3. **SSHアクセス・mysqldumpコマンドの利用可否**（DBバックアップ機構に必須）
   - 共用レンタルサーバーの場合、シェルアクセスやmysqldumpが制限されているケースがあるため要確認

→ これらが利用不可の場合、自動アップデート機構（第7章）の設計を「案B: 通知＋手動適用」または「案C: 手動デプロイ」に変更する必要があります。**設計フェーズに入る前に確認を推奨**します。

---

## 9. デザイン・UI

| 項目 | 仕様 |
|---|---|
| UIタイプ | ダッシュボード型。ただし**開いてすぐに短縮URLを発行できる**導線を確保 |
| デザインの統一 | 個人HPプロジェクトのデザインシステムとは**独立**（統一不要） |
| レスポンシブ対応 | 必須 |

---

## 10. セキュリティ方針

- CSRF対策、レート制限、SQLインジェクション対策: プロフィール記載の共通開発ルールに準拠
- スパム対策: reCAPTCHA導入
- パスワード保護リンク: ブルートフォース対策（5回誤入力で15分ロックアウト）
- 悪意URLチェック: リダイレクト時に毎回実施（3日キャッシュ）
- GitHubトークン等の機密情報: APP_KEYによる暗号化のうえDB保存

---

## 11. 今後の設計フェーズでの検討事項

- テーブル設計（ER図）の作成
- サブドメインルーティングの具体的な実装方式（Laravel内でのミドルウェア設計）
- ヘルスチェックの具体的なテストシナリオ（どこまでの機能をテストするか）
- Discord Webhook通知の具体的なメッセージフォーマット
- APIエンドポイント仕様（リクエスト/レスポンス形式）の詳細設計
- reCAPTCHAのバージョン選定（v2 / v3）
- GeoIP判定に使用するライブラリ・データベースの選定

---

## 12. 実装との差異（v26.9.5 時点）

2026-09-21 にリリースした v26.9.5 と、本書（1〜11章）との差異をまとめる。記載のない項目は要件どおりに実装している。

### 12-1. 要件から変更した点

| 章 | 要件 | 実装 | 理由 |
|---|---|---|---|
| 1 | サービス名・ドメインは chok.ooo | 誰でも設置できるオープンソース（MIT）とした。サイト名・運営者名は管理画面で設定する（未設定ならドメイン名を表示） | 汎用化 |
| 2-1 | カスタムスラッグは3〜20桁 | 管理者は1文字から使える（API・CSV を含む）。一般ユーザーは3文字から | 依頼による |
| 2-2 | 初期リストに「個別追加」（choko1229, choko, 1229, choco, tyoko）を含める | 初期リストから外し、管理画面の「予約語」で追加する | 汎用化 |
| 2-2 | 管理者は予約語もカスタムスラッグに使える | 使える。ただしサイト内ページと同じ語（`/contact` など）は使えない。「ページのURL」でページ側の URL を変えれば使える | ページが優先され、短縮URLを開けなくなるため |
| 2-4 | 編集はカスタムスラッグのみ | 本人は共有時のカードも編集できる。管理者は元URL・有効期限・発行者も変更できる | 依頼による |
| 2-7, 10 | リダイレクトのたびに毎回チェック | 限定モード（12-3）では、管理者と許可したユーザーが発行したリンクはチェックしない | 依頼による（発行者を信頼できるため） |
| 3 | 移動先と進み具合を表示し、チェック完了後に移動 | 限定モードの上記リンクは、確認画面を出さずにそのまま転送する。それ以外は要件どおり（安全と分かったら待たずに移動） | 依頼による |
| 4-2 | セットアップ画面で Discord の Client ID / Secret と DB 接続情報を入力 | セットアップ画面は DB 接続情報のみ（ドメインはアクセス中のホスト名から自動判定）。Discord のキーは初回ログイン時の設定画面で入力する（管理者が登録されるまで有効） | 入力を最小限にし、Redirect URI をドメイン確定後に案内するため |
| 7-1 | cron が1日1回チェック | サイトへのアクセスをきっかけに実行する（日本時間 4:00 以降の最初のアクセス）。cron を登録した場合は cron を優先 | cron を必須にしないため |
| 7-1 | DB ダンプは mysqldump | mysqldump が使えない場合は PHP で同等のダンプを作る | 共用サーバーで使えない場合に備えるため |
| 7-1 | git fetch → 新タグへ checkout → composer install | Git で設置した環境は要件どおり。FTP で設置した環境は、Releases に添付した zip（vendor 同梱）でコードを入れ替える。chok.ooo は zip 方式 | SSH が使えないため（12-5） |
| 7-2 | プライベートリポジトリ | 公開リポジトリ。GitHub のトークンは非公開リポジトリから更新する場合のみ必要。更新元は `.env` の `SHORTENER_UPDATE_REPOSITORY` で指定する | オープンソース化 |
| 8 | Composer・cron・SSH・mysqldump が必須（未確認） | いずれが無くても動く | 12-5 |

自動アップデートは、方式を変えずに「案A: 完全自動適用」のまま実現している（バックアップ3世代・ヘルスチェック・自動ロールバック・Discord 通知）。

### 12-2. 要件に書かれていなかった細部の決定

| 章 | 内容 |
|---|---|
| 2-1 | ランダムコードはアクセス時も大文字小文字を無視して照合する。カスタムスラッグは、既存のランダムコードと大文字小文字違いで重なる値も使えない。使える文字は英数字・`-`・`_` |
| 2-2 | 予約語は大文字小文字を無視して判定する。アプリのパス・フォルダと衝突する語（`install` `build` `vendor` など）を初期リストに追加した |
| 2-3 | 「期限が近い」表示は残り3日以内 |
| 2-4 | カスタムスラッグを変更した場合も、変更前のコードは欠番として再利用しない |
| 2-5 | パスワードは4〜72文字。ロックは「リンク × アクセス元IP」単位（第三者がリンクを締め出せないようにするため） |
| 2-6 | リファラはホスト名のみ記録し、IP と User-Agent は保存しない。クリックは中間ページの JavaScript が送信した時点で数える（リンクプレビューのボットを除外） |
| 2-7 | 危険 → 移動を中止、確認できない → 警告を表示して利用者が選ぶ、安全 → すぐに移動 |
| 4-2 | 管理者が一人もいなくなる権限変更・退会はできない |
| 4-3 | 月間件数は日本時間の暦月で数え、削除分も含める。レート制限は発行に成功した回数のみ数える。管理者もログインユーザーと同じ上限（API・CSV は対象外） |
| 4-4 | reCAPTCHA は未ログインの発行とお問い合わせにのみ適用し、Google 側の障害時は発行を止めない。退会時、発行済みの短縮URLを残すか削除するかを選べる |
| 5 | API はレート制限に加えて月間上限も適用しない |
| 7-1 | テーブルの変更は、更新後の最初のアクセスでも自動で適用する（zip を手作業で上書きした場合に備えるため） |
| 7-2 | DB 接続情報は DB 自体に置けないため `.env` に保存する（GitHub トークンは要件どおり暗号化して DB に保存） |

### 12-3. 要件に無い追加機能

| 機能 | 内容 |
|---|---|
| 限定モード | 管理者と許可したユーザーだけが発行・ダッシュボードを使える。ほかの人には「このドメインについて」（Markdown で編集）を表示するか、別の URL へ移動する。発行済みの短縮URLは誰でも開ける |
| CSV でのまとめて発行 | 管理者のみ、最大500行。月間上限・レート制限・予約語・文字数の設定は適用しない。取り込めない行は「行・列・入力された値・理由」で表示する |
| 見た目の設定 | メインカラー（プリセット＋任意の色）、ダークモード（訪問者も切り替え可）、書体5種類、サービスアイコン・ファビコン |
| ページのURL | 利用規約・プライバシーポリシー・お問い合わせ・削除ページの URL を変更できる |
| 利用規約・プライバシーポリシー | Markdown で編集し、実装に沿ったテンプレートを読み込める |
| お問い合わせ | 内容を DB に保存して管理画面で読む。Discord Webhook で通知する |
| 共有時のカード（OGP） | Discord や X に貼ったときの表示をリンクごとに選べる |
| QRコード | 発行時に加えてダッシュボード・API でも表示し、SVG / PNG でダウンロードできる |
| Web インストーラ | WordPress と同じ手順で設置できる。`.htaccess` で公開フォルダ直下にも置ける |
| アップデートの手動実行 | 「今すぐ更新する」ボタンと、通知のテスト送信 |
| 統計 | クリック数の日別推移 |
| 管理画面 | 管理者向けの画面を「管理」のタブにまとめた。ユーザー一覧に Discord のアイコンを表示する |
| ドキュメント | 設置・運用の手引きを GitHub Pages で公開（<https://choko1229.github.io/URL-Shortener/>） |

### 12-4. 11章の検討事項の決定

| 検討事項 | 決定 |
|---|---|
| テーブル設計 | `database/migrations` に実装 |
| サブドメインルーティング | 単一アプリのまま、`Route::domain()` でサブドメインごとにルートを分ける |
| ヘルスチェック | DB 接続、仮の短縮URLを発行して検索できるか（データは残さない）、ビルド済みアセットの有無 |
| Discord Webhook 通知 | 自動アップデートの成功・失敗と、お問い合わせの受信を通知する。メンションは含めない |
| API 仕様 | `POST/GET /v1/links`、`GET/DELETE /v1/links/{コード}`。`Authorization: Bearer` でキーを渡す |
| reCAPTCHA | reCAPTCHA Enterprise のスコアベース（v3 相当）。スコア 0.5 未満を拒否 |
| GeoIP | DB-IP の IP to Country Lite（CC BY 4.0）を自動で取得し、毎月更新する。MaxMind GeoLite2 を置けばそちらを優先 |

### 12-5. 8章の未確認事項の結果

| 項目 | 結果 | 対応 |
|---|---|---|
| Composer | サーバーでは使わない | 配布用 zip に vendor を同梱する |
| cron | 不要にした | アクセスをきっかけに定期処理を実行する。登録すれば cron を優先する |
| SSH・mysqldump | SSH は利用不可 | 更新は zip の入れ替え、DB ダンプは PHP で作れるようにした。mysqldump が実サーバーで使えるかは未確認 |
